<Project Sdk="Microsoft.NET.Sdk">

  <!-- Domain, ports, and pure operations. BCL only: this project takes no
       package reference and no project reference (org dependency-direction rule). -->
  <PropertyGroup>
    <RootNamespace>AccountRotation.Core</RootNamespace>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

</Project>
